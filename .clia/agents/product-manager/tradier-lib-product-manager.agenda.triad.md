# AGENDA — TradierLib Product Manager

## Near-Term

\- Maintain DocC parity across broker libs; ensure Services pages list top APIs.
\- Keep domain layout conventions stable to simplify OmniBroker adapters.
\- Add larger, realistic payloads (multi-quotes with greeks) to perf suite; persist CSV to artifacts directory in CI.
\- Add CLI wrapper to run perf outside XCTest and tag runs with build metadata.

## Mid-Term

\- Release cadence and KPIs; add docbuild checks in CI.
\- Consider optional parser injection for auth flows to keep end-to-end consistency.

## Long-Term

\- Ecosystem adoption and partnerships.
